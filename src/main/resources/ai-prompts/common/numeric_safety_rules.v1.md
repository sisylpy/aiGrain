> **【预留 / 未接入生产拼装】**
> - 当前**未被**生产 Prompt 拼装主链路引用（`AiPromptRegistry` 已登记，现网无 `require("common.*")`）。
> - 本文件属于**预留 / 草案**规则片段。
> - **不作为**当前数字安全、计算安全、回答风格的唯一权威源；同类约束已分布在各 Composer 草案与 Java Renderer 中。
>
# Prompt ID

common.numeric_safety_rules.v1

# 使用场景

公共片段占位：数字格式（禁止科学计数法、括号拆解等）的共性条文。

# 输入契约

无（尚未接入拼装管线）。

# 禁止事项

本文件当前不直接进入 LLM。

# 输出要求

无。

# Prompt 正文

（预留）
