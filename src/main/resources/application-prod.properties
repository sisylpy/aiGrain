# 生产环境示例：部署时在服务器上覆盖 app.files.images-root，或设置环境变量 APP_FILES_IMAGES_ROOT
# 勿把上传目录放在 war/jar 包内，否则每次发版会丢文件。
#
# 与老项目同机部署时：可与老项目共用同一根目录（如下路径与旧 mvc:resources 的 location 一致），
# 两边 URL 仍用 /goodsImage/** 等同前缀即可，注意两应用写文件时不要覆盖同名路径（建议上传名带随机前缀）。
app.files.images-root=file:/opt/tomcat/latest/app-data/images/
